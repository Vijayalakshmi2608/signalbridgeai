# SignalCore and GeoShield verification

The Alert Intelligence route renders the raw warning ingestion interface and structured alert panel in the existing command-center shell. The initial screenshot captured before the async raw-alert query finished, so the form showed empty input boxes; the backend contract and unit tests confirm the demo alert loads and interpretation works.

The Risk Map route renders OpenStreetMap tiles with translucent polygon warning zones, a selected user location marker, shelter markers, road overlays, safe reference markers, and a visible ACTION status. The map legend and boundary disclaimer are visible.

The dashboard route showed its intended loading skeleton during the immediate capture window after the backend restart; the route remains covered by existing snapshot tests and the API contract is intact.


After allowing the server to settle, the Alert Intelligence form hydrated with the full simulated raw warning (alert ID, hazard, source, area, issue/validity times, and warning text), and the dashboard hydrated with active warning data and refreshed action/community metrics.


The full-page Alert Intelligence capture verified the complete flow: raw ingestion form, structured alert object, extracted guidance, explicit DEMO INTERPRETATION label, register table, and one source-provenance panel per alert.


At 390×844, the upgraded routes remain usable: the sidebar collapses to the menu button, the alert form stacks fields cleanly, and the risk map keeps the selected location marker and ACTION state visible within the mobile card.
